export default function Page() {
    return (
        <div>
            <p>
                Self designed on the go. Coded in (LazyVim). Built with
                (Next.js), (TypeScript) and (Tailwind CSS). Deployed with
                (Vercel). All text is set in the Geist typeface.
            </p>
            <p>List of inspirations</p>
        </div>
    );
}
